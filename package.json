{
  "name": "tianxing-plc-selector",
  "version": "1.0.0",
  "private": true,
  "description": "天行 PLC 选型配置前端",
  "scripts": {
    "check": "node --check app.js && node --check admin.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
